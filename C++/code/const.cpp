#include <iostream>

int main(){

    //const stands for constant and the value of the variable we assign with const can't be changed
    //If it is changed by someone later in the program it will just cause an error
    //EXAMPLE:

    const double PI = 3.14159;
    const int LIGHT_SPEED = 299792458;
    const int WIDTH = 1920;
    const int HEIGHT = 1080;

    return 0;
}